import { Breakpoint } from '../models/breakpoint';

/** Valores que definen el width cada breakpoint. */
export const BREAKPOINTS: Record<Breakpoint, number> = {
  xxs: 0,
  xs: 576,
  sm: 768,
  md: 992,
  lg: 1200,
  xl: 1440,
};
